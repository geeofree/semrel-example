/**
 * Adds two numbers
 * @param {number} a
 * @param {number} b
 * @returns {number} a + b
 **/
const add = (a, b) => a + b;

/**
 * Subtracts two numbers
 * @param {number} a
 * @param {number} b
 * @returns {number} a - b
 **/
const sub = (a, b) => a - b;

module.exports = {
  add,
  sub,
};
